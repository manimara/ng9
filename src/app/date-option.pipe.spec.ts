import { DateOptionPipe } from './date-option.pipe';

describe('DateOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
